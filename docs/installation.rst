============
Installation
============

At the command line::

    $ easy_install sqlite_browser

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv sqlite_browser
    $ pip install sqlite_browser